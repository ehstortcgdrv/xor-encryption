# xor-encryption

Esta é uma implementação de criptografia de strings em tempo de compilação.

## Demonstração (IDA Pro)
Abaixo, a comparação mostrando que a string protegida com `xorstr_` não aparece no executável:

![Demonstração do XOR](assets/animation.gif)
